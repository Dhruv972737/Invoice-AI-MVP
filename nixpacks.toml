[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['rm -f package-lock.json', 'npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'