[phases.setup]
nixPkgs = ['nodejs_20', 'nodePackages.npm']

[phases.install]
cmds = ['npm ci --no-cache']

[phases.build]
cmds = ['npm run check-node && npm ci --no-cache && npm run build']

[start]
cmd = 'npm start'