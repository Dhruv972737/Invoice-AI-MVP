[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['rm -f package-lock.json', 'npm install --omit=dev --no-audit --no-fund']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'